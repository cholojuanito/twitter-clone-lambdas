class Mention {
    public route:string;
    public alias:string;

    constructor(route:string, alias:string) {
        this.route = route;
        this.alias = alias;
    }
}

export default Mention;